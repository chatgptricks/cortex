# Server deployment requirements (Render/cloud).
# Excludes tribev2 + nilearn: analysis runs on the remote Modal GPU
# (REMOTE_TRIBE_URL), so the API server never loads the brain model locally.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9,<1.0
pillow>=10,<13
imageio-ffmpeg>=0.5,<1.0
huggingface-hub[inference]>=0.31,<2.0
httpx>=0.27,<1.0
numpy>=2.0,<3.0
pandas>=2.0,<3.0
scikit-learn>=1.4,<2
